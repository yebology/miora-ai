meta {
  name: Analyze Wallet (Solana)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/wallets/analyze
  body: json
  auth: none
}

body:json {
  {
    "address": "{{solanaAddress}}",
    "chain": "solana",
    "limit": 25
  }
}
