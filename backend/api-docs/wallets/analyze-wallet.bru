meta {
  name: Analyze Wallet
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/wallets/analyze
  body: json
  auth: none
}

body:json {
  {
    "address": "{{walletAddress}}",
    "chain": "{{chain}}",
    "limit": 25
  }
}
