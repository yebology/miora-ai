meta {
  name: Analyze EVM
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/wallets/analyze
  body: json
  auth: none
}

body:json {
  {
    "address": "{{walletAddress}}",
    "chain": "{{chain}}",
    "limit": 10
  }
}
