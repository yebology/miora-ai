// Package constants holds default values for the AI trading agent.
package constants

const (
	// DefaultAgentStatus — agent starts paused, user must explicitly start after configuring.
	DefaultAgentStatus = "paused"
)
